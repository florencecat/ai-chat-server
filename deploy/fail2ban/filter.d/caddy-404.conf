# Массовые 404 — перебор путей. Легитимный клиент (мобильное приложение)
# ходит только на известные маршруты и 404 почти не получает.
[Definition]
failregex = ^.*"remote_ip":"<HOST>".*"status":404\b
ignoreregex =
datepattern = "ts":{EPOCH}
